apiVersion: apps/v1
kind: Deployment
metadata:
  name: pitstop-webapp-deployment
  namespace: pitstop
  labels:
    app: pitstop-webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pitstop-webapp
  template:
    metadata:
      labels:
        app: pitstop-webapp
    spec:
      containers:
        - name: pitstop-webapp
          image: dahami03/pitstop-webapp:1e0f316074e929aaf785d0d83a7b431bae772b96
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: pitstop-webapp-configmap
